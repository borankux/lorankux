pipeline {
  agent any
  stages {
    stage('PreBuild') {
      steps {
        sh '/usr/local/bin/terminal-notifier -title ProjectX -subtitle "Prebuilt" -message "Local Jenkins Pre built" -appIcon http://vjeantet.fr/images/logo.png'
      }
    }

  }
}